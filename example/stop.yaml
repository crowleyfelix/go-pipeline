id: 'stop-example'
description: Demonstrate conditional pipeline stop behavior.
steps: 
- type: stop
  params:
    condition: '{{ gt 2 1 | and (eq "true" "true") }}'
    message: 'Stopping pipeline'
    #is_error: 'true'